from django.conf.urls import url

# from api.sms import sendCode

urlpatterns = [
    # url(r'^<uid>', UserView.as_view()),
    # url(r'^v1/user/reg', register),
    # url(r'^v1/sms/code', sendCode),
]